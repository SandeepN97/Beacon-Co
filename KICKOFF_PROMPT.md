# Historical kickoff prompt

This prompt initiated the original single-file HTML-to-Astro migration. That migration is complete.

Do **not** use the old kickoff instructions for current work. They describe an unscaffolded project, incomplete section migration, and checkpoints that no longer match repository reality.

For current work:

1. Read `AGENTS.md`.
2. Start with `docs/index.md`.
3. Verify implementation state in `docs/current-state.md`.
4. Check `docs/decisions/index.md` before changing a significant direction.
5. Follow `docs/governance/change-process.md`.

The original migration source remains at `reference/v9-source.html` for content and behavior comparison.
