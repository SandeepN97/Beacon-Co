---
interface Props {
  state?: string;
  label?: string;
}
const { state = "current", label } = Astro.props;
---
<aside class={`truth-notice truth-notice--${state}`}>
  <div>
    <span class="truth-notice__label">{label ?? state}</span>
    <slot />
  </div>
</aside>
