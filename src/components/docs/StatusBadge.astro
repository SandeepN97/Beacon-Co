---
interface Props {
  value: string;
}
const { value } = Astro.props;
const slug = value.toLowerCase().replace(/[^a-z0-9]+/g, "-");
---

<span class={`docs-status docs-status--${slug}`}>{value}</span>
